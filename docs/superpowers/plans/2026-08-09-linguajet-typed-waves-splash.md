# LinguaJet 打字波浪启动页实施清单

**目标：** 让启动页通过“打字、停留、删除、再打字”传达语言学习的开始感，并用克制的动态波浪补足背景层次。

**范围：** 仅处理启动页和通用文字动画组件；日常学习页面的配色、导航和内容结构不变。

## 任务 1：补充检查

- [x] 在 `src/components/TextType.test.jsx` 中验证：第一句会删除后才显示第二句。
- [x] 在 `src/App.test.jsx` 中验证：欢迎文字采用无空格的产品文案，并含有波浪背景容器。
- [x] 运行相关检查，确认旧实现无法满足新流程。

## 任务 2：实现启动页体验

- [x] 安装 `ogl`，新增隔离的 `GradientWaves` 背景组件；无 WebGL 环境时保留安全的空背景。
- [x] 扩展 `TextType`：支持文字数组、1 秒停留、末尾逐字删除、最终完成回调和始终跟随文字末尾的闪烁光标。
- [x] 改造 `SplashScreen`，按顺序使用两句文案，并将波浪置于文字后方；减少动态效果偏好下显示静态背景与完整文字。
- [x] 运行相关检查，确认通过。

## 任务 3：整体检查

- [x] 检查桌面和手机尺寸下的文字可读性、波浪层次、点击进入、导航和词表流程。
- [x] 运行完整测试、规范检查、生产打包、界面规则扫描和差异检查。
