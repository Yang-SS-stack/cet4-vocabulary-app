# 四级单词学习

这是一个本地使用的四级单词学习网页，目前包含基础页面外壳和导航。

## 本地打开

在项目目录运行：

```powershell
npm run dev
```

然后打开命令行显示的本机地址。

## 检查项目

```powershell
npm test
npm run lint
npm run build
```

- `npm test`：检查导航切换是否正确。
- `npm run lint`：检查代码写法。
- `npm run build`：检查网页能否打包为可运行文件。
